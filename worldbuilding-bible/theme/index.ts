import { defineShikiSetup } from '@slidev/types'

export default {
  name: 'soviet',
  // Default configuration
  defaults: {
    fonts: {
      sans: 'PT Sans',
      mono: 'PT Mono',
    },
    highlighter: 'shiki',
  },
}
